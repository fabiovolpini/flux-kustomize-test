apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: monitoring
spec:
  rules:
    - host: grafana.localhost
      http:
        paths:
          - path: /
            backend:
              serviceName: grafana
                servicePortport: 3000
